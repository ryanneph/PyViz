import sys

## Curently it is necessary to inform this project of the location where the separate PyMedImage library is installed
## This can be done by setting the path to the git repository obtained by the command:
##
## git clone
path = '../PyMedImage'
sys.path.insert(0, path)
